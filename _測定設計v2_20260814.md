# 測定設計 v2 — 攻撃されて残ったものだけでできた構造 (2026-08-14)

**「完璧に穴のない構造」とは、4本全部が生き残る構造のことではない。**
**攻撃して、死んだものを捨て、残ったものだけで組んだ構造のことである。**

昨日までの扉は「自分にだけ甘い」穴を持っていた。今日の4軸案は「測ったより大きいことを言う」穴を持っていた。**この文書は、その2種類の穴を仕組みで塞いだ後に残る全部である。**

---

# 第1部 測定憲法（8条）

これから扉に足すすべての測定は、この8条を全部満たす。**1条でも落ちる軸は、どれだけ魅力的でも載せない。**

| # | 条 | 理由 |
|---|---|---|
| 1 | **順序を持つ出力を公開しない。** 許すのは boolean・日付付き事象列・ハッシュ・**分母つきの実数**（「52回中47回」）のみ。割合・中央値・連続日数・回数集計は禁止 | 比較可能なスカラーを並べた時点で順位である。順位は買える。最初のスクレイパーが並べ替えて、こちらを出典に leaderboard を作る |
| 2 | **三値を混ぜない。** measured-pass / measured-fail / **not-measured** を全データ構造で区別する。測っていないものは、誰の合格にも**不合格にも**ならない | 扉の fail-open 事故の一般化。「載っていない=悪い」も「測れなかった=落ちた」も同じ穴 |
| 3 | **良い軸の署名: 偽装を通す唯一の方法が、偽装をやめることであること。** この署名を持たない軸は載せない | 修復速度は「壊して直す自作自演」で通せた。署名を持つ軸（提示の一貫性・ランダムトークン）は、防御=正しい実装になる |
| 4 | **単発の観測から事実を作らない。** 失敗は独立に再確認されるまで記録しない | 1リクエストの失敗率は0.1〜1%ある。年1〜4回の「捏造された障害日」を、名指しの他社について永久に公開することになる |
| 5 | **こちらの障害を相手の記録にしない。** 毎掃引の最初にカナリアを測り、カナリアが落ちたら掃引全体を VOID。**カナリアは登録簿に載っているサーバーであってはならない** | Cloudflare の cron は best-effort。こちらの障害日が全登録者の「障害日」として永久に残る。カナリアが自社サーバーだと、自社障害の日に記録が止まる=自己都合の隠蔽になる |
| 6 | **同意の精神。** 故意に不正な入力は opt-in なしで送らない。「ツールを呼んでいないから」は言い訳にしない | 不正なメソッド名の日次送信は、相手のエラーログを汚し、SLO を焼き、偵察と区別がつかない |
| 7 | **予算は設計段階で計算する。** 無料枠は 50 subrequests/invocation。現状 15×3=45。**1本足す前に掛け算をやり直す** | D をそのまま足すと 15×4=60 で本番の掃引が壊れていた。レビューがデプロイ前に捕まえた |
| 8 | **成熟を罰しない。** WAF・レート制限・認証必須・scale-to-zero・カナリア配信は正しい工学。ACCESS_DENIED / RATE_LIMITED を UNREACHABLE と混ぜない | 4軸案は全部、本番の成熟度を欠陥として読んでいた。「見栄えのためにセキュリティを緩めろ」という圧力をこちらがかけることになる |

---

# 第2部 構成要素（5つ + 削除1つ）

## E1. 適合5条件 — 現行のまま

変更なし。1点だけ patch9 に同乗させる: `summarise()` の `reason.slice(0, 240)` → `400`。（`allow_tool_call` の直し方の説明が、直し方の直前で切れている。バッジのリンク先になった今、一番人目に付く場所で切れている）

## E2. 到達記録 — 旧「可用性」。改名・再構築

**名前**: `probe reachability from a single vantage`（可用性とは呼ばない）

**出力の形（順序なし・分母つき）:**

```json
"reach": {
  "sampled": 52,
  "reached": 47,
  "not_reached": [
    { "at": "2026-08-02T18:23:11Z", "class": "CONNECT_TIMEOUT",
      "confirmed_by": 3, "colo": "NRT" }
  ],
  "access_denied": [ { "at": "...", "class": "HTTP_403_CHALLENGE" } ],
  "void_sweeps": 1,
  "note": "Counts, not a rate. One sample is one instant from one vantage. This is probe reachability, not availability, and a well-formed responder in front of a dead backend passes it."
}
```

**規則:**

1. **記録前確認**: 初回失敗は KV に `suspect` として置くだけ。**+10分・+20分の確認パスが再測定し、3回連続で失敗して初めて `not_reached` に記録**（`confirmed_by: 3`）。1%の瞬断が 1e-6 になる。既存の `unreachable_streak: 3` と同じ思想
2. **ジッタ**: 掃引内の各エンドポイントの実行分を `HMAC(secret, endpoint‖date)` から導出。**実行後に実時刻を公開**（方法は透明、瞬間は予測不能）
3. **カナリア**: 毎回最初に中立の外部2点（例: `www.cloudflare.com/cdn-cgi/trace` ともう1点）。両方落ちたら掃引 VOID
4. **失敗を分類**: DNS / TLS_HANDSHAKE / CONNECT_TIMEOUT / HTTP_4XX / **HTTP_403_CHALLENGE** / **HTTP_429** / HTTP_5XX / GATE_ERROR。**403 と 429 は reached にも not_reached にも数えない。別の欄**
5. **colo を記録**: cron の Worker は毎回同じ場所から出ない。地域限定サービスの見え方がこちらの都合で揺れることを、記録自身が説明できるようにする
6. **streak はサンプル数でのみ**。日数に換算しない（週1層の「2連続」は8〜21日のどこか。整数の日数は無い精度の主張）

**正直に書いておく限界**: 静的な握手シム（死んだバックエンドの前に置いた20行の応答機）はこの軸を永久に通す。**ツールを呼ばずにこれを見破る方法は存在しない。** だから記録の note に「a well-formed responder in front of a dead backend passes it」と書く。**限界を主張と同じ大きさで。**

## E3. 表面の変更履歴 — 旧「安定性」。指標を捨て、事実だけ残す

**「安定」という言葉・変化回数・無変化日数・割合は全部使わない。** 放置サーバーが満点を取る指標だから。MCP 仕様自体が `notifications/tools/list_changed` を持っている — 仕様の作者はツール一覧の変化を正常運用と見なしている。

**測る対象を名前からマニフェスト全体へ**: 各ツールの `name + description + inputSchema` を JCS 正規化して SHA-256、ソート済み集合のハッシュを取る。`initialize` の `serverInfo` / `capabilities` も同じ要求内でハッシュ（追加コストゼロ）。

```json
"surface": {
  "names_hash": "ab12…", "manifest_hash": "cd34…", "server_info_hash": "ef56…",
  "tool_count": 14, "pages_followed": 1, "complete": true
}
```

**変化があった日だけ、中立な文で事象を1行:**

```json
{ "at": "2026-09-04T18:12:00Z", "kind": "surface_change",
  "classes": ["input_schema_changed"],
  "added": [], "removed": [], "schema_changed": ["get_price_range"],
  "operator_note": null }
```

`operator_note` は運営者が申告すれば**そのまま**載せる（判定しない。公開するだけ）。

**★必須の防御**: `tools/list` はカーソルでページングされる。**カーソルを最後まで辿り（上限3ページ）、辿り切れなかったら `complete: false` で surface 全体を not-measured にする。部分的な読み取りから「削除」を出したら、「14本のツールが消えた」という誤報を名指しで公開することになる。**

**これで塞がる穴**: 名前を残したまま inputSchema を書き換える偽装（現実の統合破壊の第1位）が、コストゼロから「マニフェスト全体を凍結し続ける」コストに変わる。

## E4. 提示の一貫性 — ★新設。本命

**同じ read-only の対象（`initialize` / `tools/list` / agent card）を2視点から取り、正規化してバイト比較する。**

- (a) 名乗った扉（Cloudflare Worker。patch10 で UA を名乗らせる）
- (c) **名乗らない一般クライアント、Cloudflare の外から**（GitHub Actions の cron。無料）

**★2026-08-14 19:54 実測により、当初あった視点(b)「Worker から匿名」は削除された。**
webhook.site を扉に測らせ、届いたヘッダを観測した結果:

```
cf-worker: horizonshield.dev      ← Cloudflare がインフラ層で付ける。UA と無関係
cdn-loop:  cloudflare; loops=1
egress IP: 172.71.8.8（Cloudflare帯・東京）
user-agent: 無し（扉は現在、名乗らずに他人を測っている）
```

**Worker からの匿名は原理的に不可能**（ゾーン名が常に相手に渡る）。匿名視点は Cloudflare の外にしか置けない。
副産物2つ: どうせ割れるのだから**名乗ることにコストが無い**（設計の迷いが1つ消えた）。そして**他人に開示を要求する扉が、自分は無名で測っている** — patch10 で UA を名乗らせる（`HORIZON-SHIELD-verify-gate/x.x (+https://gate.horizonshield.dev/spec)`）。SentinelOracle ですら opt-out URL 付きで名乗っていた。

**出力は boolean 1つ + 差異の種別。** 順序なし。

```json
"presentation": {
  "consistent": true,
  "views": ["worker-labeled", "external-anonymous"],
  "compared": ["initialize", "tools/list", "agent-card"],
  "diff_classes": []
}
```

**なぜ本命か**: 憲法3条の署名を唯一、完全に持つ。**正直な運営者は何もせずに通る。細工（UA での出し分け、扉の特別扱い、掃引時刻だけの起動）は全部この軸で自己申告になる。通す唯一の方法が、細工をやめることである。**

**(c) が必須である理由（実測済み）**: `cf-worker: horizonshield.dev` が実測で確認された。UA を変えても扉は割れる。よって匿名視点は Cloudflare の外にしか存在できない。

**透明性との整合**: 名乗らない視点の存在自体を方法論に明記する。**方法は透明、個々の実行だけが予測不能** — ジッタと同じ理屈。特定の第三者へのなりすましはしない。

**GitHub Actions 側の仕事**: 日次 cron + `sleep $((RANDOM % 3600))` で時刻を散らし、汎用 UA で3対象を取得 → 正規化ハッシュを gate の受け口（token 付き POST）へ報告。workflow ファイルはリポジトリに置く=方法の透明性がそのまま担保される。**ついでに E2 の到達確認を外部視点でも1回/日実行**（掃引時刻ゲーミングへの実質的な対抗はこっち）。

## E5. エラーの形 — opt-in 専用。一律の列から削除

- **登録時に `probe_error_shape: true` を明示した相手だけ**（憲法6条）
- **プローブのメソッド名は毎回ランダム**（`HMAC` 由来の16hex）。公開ソースの定数だと if 文1行で永久に突破される。ランダム化すると、**残る唯一の防御が `if (!known.includes(m)) return -32601` = 正しい実装そのもの**になる（憲法3条の署名）
- **判定は明白なときだけ**: JSON-RPC エラーオブジェクト（code 付き）が返れば pass、それ以外は **FAIL ではなく not-measured**。401（認証必須=正しい）、400（セッション必須=正しい）、SSE 応答、ゲートウェイ整形、全部 not-measured
- **全文を記録**: 送った要求と、返った status/headers/body（≤2KB）をそのまま履歴に載せる。読者が自分で判定できる
- **頻度は表面変化があったときだけ。** エラーの形は日次で変わるものではない

## 削除: B 修復速度

**実装しない。集計値を作らない。** 日付付きの flip 事象は既に history にあり、それをそのまま見せる。理由の要約:

1. 分解能の床=サンプリング間隔。20分の修理が「1日」（週1層なら「7日」=500倍の誇張）として永久に公開される
2. **養殖できる**: 壊して直す自作自演で「迅速な運営者」の記録が作れる。一度も壊さない運営者は n=0 で空欄
3. **この事業の正直さを罰する**: 扉自身の `pending`（測れないものを合格にしない）が「無限に伸びる未修復」=最悪の記録になる。「こちらの検査に異議がある」を「あの会社は怠慢」に変換する装置になる

---

# 第3部 予算とシャード（憲法7条の実行）

**現状**: `MAX_PER_SWEEP = 15`、1本あたり3呼び出し（initialize / tools/list / card）= **45 ≤ 50**。

**v2 での1本あたり最悪値**: initialize(1) + tools/list ページ(≤3) + card(1) = **5**。15本なら 75 で**枠を超える**。

**解**: 掃引を複数の cron 発火に分割する。**発火ごとに 50 の枠が新しく付く。**

```jsonc
"triggers": { "crons": ["0 18 * * *", "10 18 * * *", "20 18 * * *",
                         "30 18 * * *", "40 18 * * *", "50 18 * * *"] }
```

- 各発火: カナリア(2) + 本測定 **5本** × ≤5 = 27 + suspect 再確認(≤6) = **≤35 ≤ 50** ✓
- KV の `sweep:cursor:<date>` で続きから歩く。6発火 × 5本 = **30本/日の容量**（現在の監視6本 + 有料枠に十分）
- **+10分後の発火が、前の発火の suspect を再確認する** — 記録前確認（憲法4条）がシャード構造から自然に出る

---

# 第4部 いま動かす1手 — `CF-Worker` の実測（5分・コード変更なし）

E4 の設計は「`CF-Worker` ヘッダが本当に漏れるか」で根本から変わる。**ドキュメントではなく自分のトラフィックで確定させる。**

**手段は既に持っている**: hs-mcp は observability 有効で、扉は hs-mcp を測れる。

ターミナル1:

```bash
cd ~/Desktop/hs-docfix/workers/hs-mcp
npx wrangler tail --format json
```

ターミナル2:

```bash
curl -s -X POST https://gate.horizonshield.dev/check -H 'content-type: application/json' -d '{"endpoint":"https://mcp.horizonshield.dev/mcp"}' -o /dev/null
```

ターミナル1に流れた JSON の `event.request.headers` を見る。

| 見つかったもの | 意味 | E4 の設計 |
|---|---|---|
| `cf-worker: <ゾーン名>` がある | **扉は名乗らなくても割れる** | 視点(c)は Cloudflare の外（GitHub Actions）が**必須** |
| 無い | UA だけで匿名化できる | それでも egress IP 帯（Cloudflare 公開レンジ）で割れるので、(c) は外部推奨のまま |

ターミナルを2枚開くだけ。**どちらの結果でも E4 は作る。変わるのは (b) の視点に意味があるかどうかだけ。**

---

# 第5部 実装順

| # | 名前 | 中身 | 前提 |
|---|---|---|---|
| 1 | ~~CF-Worker 実測~~ | **完了 (2026-08-14 19:54)。cf-worker は漏れる。視点(b)削除** | — |
| 2 | **patch9** | E3（マニフェストハッシュ＋カーソル完走＋ページ予算ガード）+ reason 240→400 | なし |
| 3 | **patch10** | E2（シャード6発火・ジッタ・suspect 再確認・カナリア VOID・失敗分類・colo） | patch9 |
| 4 | **Actions workflow** | E4 の視点(c) + 外部からの到達確認 | 実測1の結果 |
| 5 | **patch11** | E4 の突き合わせと publish、E5 opt-in | 3と4 |
| 6 | **ページ更新** | verify-directory に憲法8条を載せる。**削った B と、削った理由も載せる**（「作りかけて、養殖できると分かって捨てた」は、この事業で一番強い宣伝でもある） | 2〜5 |

**B はどの段階でも実装しない。**

---

# 第6部 残る限界（正直に、主張と同じ大きさで）

1. **静的握手シムは E2 を永久に通す。** ツールを呼ばずに見破る方法は無い。記録に明記する
2. **ジッタの窓は有界**（18:00–18:50 UTC）。窓ごと稼働させる偽装は残る。E4 の外部視点のランダム時刻が実質の対抗
3. **E4 は「2視点に同じものを見せた」ことしか言わない。** 両視点に同じ嘘を見せることは防げない。防げるのは「視点によって出し分ける」ことだけ
4. **無料層の週1と有料層の日1で、記録の解像度が違う。** 「解像度を売っている」と読まれうる。分母を常に表示することで、比較不能であることを見えるようにする（90.4% ではなく 47/52 と書く理由の半分はこれ）
