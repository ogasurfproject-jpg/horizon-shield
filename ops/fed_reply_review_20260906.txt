Your branch is on main as your commit, 18862488, history intact. One commit of mine follows it: the nine em dashes in your two sections replaced with commas, semicolons, a colon and one pair of parentheses, nothing else touched. Diff it against yours and tell me if any of the nine moved your meaning.

Table 3 is better than mine. My separator row was wrong (Python with indent=2 emits a comma and a newline, not a comma and a space), the data-derived keys point is right and narrower than I had it, and "unverified" on the chain-hash row is the correct word where I had "latent".

Three places where the text says more than the record supports. Two of them are also my overclaims in the abstract, so they get fixed together, not by you alone.

1. 4.2 says make_ring.py was never opened, fetched, or consulted in any form, before, during, or after, and that its path and name were learned only from the anchored record afterward. The register README's 2026-09-05 entry, written from your own message at 17:49 JST, says you cloned the register and ran scripts/make_ring.py --verify on all eight rings before the reimplementation. Every ring file also names the script in its recompute field. And the spec hash you checked is printed in two places, the docstring at the head of make_ring.py and the ledger's own entry for the spec; which one you read against matters for the sentence. The narrower claim is still strong: the reference was executed as a black box in the earlier recompute, and its body was not read before or during the reimplementation. If that is what happened, write that. If not, write what did. Only you know.

2. 4.2 says the build started from the spec's Layer 3 section and this paper's Section 3, and nothing else. This paper did not exist yet: its first commit is 23:35 JST on 9/5, your make_ring.js commit is 13:08:01Z, which is 22:08 JST. And the spec's Layer 3 sketch has eleven fields where a ring has twenty. first_instant, last_instant, instants_by_status, instants_by_consent_source, record_sha256_first, record_sha256_last, witness_identities, prev_ring, recompute, and the byte rules are not in the spec at all. So the field set and the canonical form came from somewhere: the published ring files, the two READMEs, the verify output at 17:49. Say which. It matters twice. Condition (d) shifts a little, because you had seen the shape of the outputs before running blind. And Section 6 gets sharper, because the spec is not runtime-defined, it is silent, and the published artifacts carried the spec. That is a better finding than the one we have now. My side of the same error: the abstract's "no access to the reference source" and "from the anchored specification alone", and 3.3 and 7 with them. I correct those in the same revision.

3. Section 6 says 3.3 defines the canonical form by naming a language and a function call. 3.3 of this paper states the rules first, sorted keys at every level, two spaces, raw non-ASCII, one trailing newline, and names Python second. The document that names nothing is NENRIN_SPEC_v1.md itself: no json.dumps, no sort, no canonical anywhere in it. One possible sentence: "The specification does not state the canonical form at all; it lives in the reference builder's docstring and the ring-v1 README, and Section 3.3 of this paper is its first language-neutral statement." Your section, your wording.

Push the fix to the same branch and I merge again. No hurry past September.

Toshikatsu
