# フェデリコへの依頼文 — 2026-08-14

## なぜこの人が正しいのか（推測ではなく記録から）

**Federico Blanco Sanchez-Llanos** は、この依頼にとって特別な立場にいる。

1. **すでに一度、自発的に外部検証をやっている。** JIDEC Path 仕様 v1 の本文にこう書いてある:

   > "When Federico fetched `/canary`, then fetched the anchored entry #4 source, then compared the constant in both, he walked a specific path through the system."

   **その「歩き」が、jidec-path-v1 という仕様そのものの動機になった。** 頼まれてやったのではなく、勝手に検証して、それが設計を生んだ。

2. **彼の指摘が本番に載っている。** `verification-contract.json` の changelog 0.2:

   > "contract and ruleset {id, version} folded INTO signed_payload (hashed). Prevents silent reinterpretation of old claims under a newer ruleset. **Prompted by Federico Blanco Sanchez-Llanos.**"

   **2026-08-14 時点で本番配信を実測確認済み。** 名前は credits と changelog の2箇所に入っている。

つまりこれは冷たい依頼ではない。**前にやってくれたことの続き**であり、**彼の指摘が形になった報告**でもある。

---

## ★頼みごとは1つだけにする

引き継ぎ (§10) には、フェデリコ宛の未処理が**3つ**ある:

| # | 用件 | 今回入れるか |
|---|---|---|
| 1 | `curl POST /check` を外部ネットワークから叩いてもらう | **入れる** |
| 2 | **$15 を1回払ってもらう**（PayPal 決済の実測・§8B 未着手） | **入れない** |
| 3 | ディレクトリを送る | **入れない** |

**3つ並べたら、今日 HN で flag された形とまったく同じになる。** 「毎回こちらの用件を持ち込む人」。1通1件。**決済テストは、返事が来てから別便で頼む。**

---

## 本文（英語・そのまま送れる）

```
Subject: One curl from your network, if you have a minute

Federico,

Two things since we last spoke.

Your point about ruleset identity shipped. The contract now folds
{id, version} into the signed payload instead of carrying it alongside, so an
old claim cannot be silently reinterpreted under a newer ruleset. It went out
as contract 0.2 and you are named in the changelog:

  https://mcp.horizonshield.dev/.well-known/verification-contract.json

Second, the gate now fails its own test. It had been returning pass: true for a
condition it openly could not measure — a Cloudflare Worker cannot fetch its own
hostname from inside its own account — but only when checking itself. Every
other server got fail-closed on an unmeasured condition. I removed the
exception. Its own verdict is "pending" now, and nothing on the register passes:
four rows, zero green, all of them mine.

The favour, and it is a small one. Every measurement I have was taken from my
own machine on my own network. That is a real weakness in the record and it is
not one I can fix from here.

  curl -s -X POST https://gate.horizonshield.dev/check \
    -H 'content-type: application/json' \
    -d '{"endpoint":"https://mcp.horizonshield.dev/mcp"}'

Read-only, no key, no account, and it calls no tools on the endpoint it is
pointed at. If what comes back differs from what I see, that is the single most
useful thing anyone could tell me right now. If it matches, I would like to be
able to say the check has been reproduced off my own network — and to name you
as the person who ran it, if you are willing.

No rush, and no hard feelings if you would rather not.

Toshikatsu
```

---

## この文が守っている規則（今日 flag の原因として特定した3つの型）

| 型 | 回避のしかた |
|---|---|
| **型1: 相手を評してから始める** | 1文目が "Two things since we last spoke." — 事実。前置きゼロ |
| **型2: 建設30年に振る** | **一度も出てこない。** この依頼に関係ないから |
| **型3: 格言で終わる** | 最後は "No rush, and no hard feelings if you would rather not." — **断る自由を渡して終わっている。** 対句なし |

**そして最初に彼の貢献の報告を置いて、頼みごとを後に置いている。** 逆にすると、貢献の報告が頼みごとの前置きに見える。

---

## 送る前に確認すること

- **連絡手段が引き継ぎに書かれていない**（§10「連絡手段はこのファイルに無い」）。メール／GitHub／Discord のどれで届くかは TOshi しか知らない
- **チャンネルによって整形を変える**: メールならこのまま。Discord/GitHub なら Subject 行を消して、`curl` を ```` ``` ```` で囲む
- **$15 の話は書かない。** 返事が来てから別便
