# HORIZON SHIELD / Yakumo 引き継ぎ（2026-08-09 版）

**このファイル1つで完結する。8/8版は破棄してよい。**

**作成**: 2026年8月9日（日）午後
**対象範囲**: 8月9日の全作業。8/8版の内容のうち、今日の実測で覆ったものは本文中で明示して訂正した
**使い方**: 新しいチャットで、このファイルを最初に読ませれば続きから動ける

---

## 登場人物

- **TOshi** ... 大賀俊勝。The HORIZ音s株式会社の代表。deploy・push・KV書き込み・git操作は全てTOshiの手
- **番人** ... アシスタント（あなた）の呼び名。コードを作り、検証し、TOshiに渡すところまで

## 新しいチャットで最初にやること

1. **セキュリティの未完了事項を最初に確認する。** 9章のパスワード変更が済んだか、TOshiに聞く。**8/8に一度、8/9に11回聞いて、いずれも回答を得られていない。**
2. このファイルを全部読む
3. 「0. 番人がまず守ること」を守ると声に出して確認する
4. **作業対象の実物を必ず開いて確認する。** エンドポイントは `curl`、ファイルは開いてから話す

**この引き継ぎに書いていない過去の経緯が必要になったら、推測で埋めずにTOshiに聞く。**

---

# ★ 8/9に前提がひっくり返ったもの（最重要）

**8/8版の引き継ぎには、事実でない記述が複数あった。同じ穴に落ちないよう先頭に置く。**

| 8/8版の記述 | 8/9の実測 |
|---|---|
| 「毎日 JST 03:00 に自動再測定」 | **一度も走っていなかった。** 実装しただけで、履歴は0件だった |
| 「カスタムドメインを当てれば解消するはず（未検証）」 | **解消する。実測で確認済み** |
| 「社名は The HORIZ音s株式会社。音は漢字、ローマ字にしない」 | **半分正しい。**登記名は音が漢字（国税庁で確認）。ただし**海外向け英語は HORIZONs で確定**（TOshi判断） |
| 「15本の設定にプレースホルダーがあり危ない」 | **1本（hs-kira-line）はすでに事故済みだった。**本番の環境変数が置換用文字列のまま |
| 「hs-kira-line の LINE 通知」 | **一通も飛んでいなかった。**宛先が置換用文字列だった |
| 「Glama はレジストリから自動で拾う」 | **拾わない。**`frank@glama.ai` に手で再インデックスを依頼する運用 |

**教訓は1つ。「実装した」と「動いている」は別。/sweep/last のような、走ったことを自分で証明する計器を必ず持たせる。**

---

## 用語（初見で詰まるもの）

- **JCCDB** ... 自社公開の建設単価オープンデータ。95,403行・CC BY 4.0・DOI登録済み
- **MCP / A2A** ... AIが外部ツールを呼ぶ規格 / エージェントが名乗り合う規格
- **KV** ... Cloudflare Workers KV
- **fail-closed** ... 測れなかったものは合格にしない
- **held / pending** ... 8/9新設。`held`=到達できず測れなかった。`pending`=届いた上で条件未達。**この2つを混ぜると監視商品が死ぬ**
- **論文7814** ... engrXiv、DOI `10.31224/7814`、CC BY 4.0

---

# 0. 番人がまず守ること

## 絶対に破ってはいけない環境の罠

- **本番のgit rootは `~/horizon-shield` のみ**
- **番人からのマウントパスは `/sessions/<session>/mnt/horizon-shield`。** `/Users/oogatoshikatsu/horizon-shield` では device_bash から見えない
- **貼らせるコードブロックに `#` コメントを入れない。** TOshiのzshはインタラクティブコメント無効
- **コードブロックにはコマンド以外を入れない。** ★8/9に番人が3回破った。Worker名の一覧・JSON設定をコマンドと同じ見た目で出し、TOshiがターミナルに貼った。**一覧は必ず表に入れる**
- **コマンドは1回に1つ**
- **ダッシュ（em/en/bar）は出力に一切使わない。** ハイフン・〜・⇔・㎡ は可
- **番人はリポジトリで `git` を実行しない。** `.git/index.lock` が残る
- **`git add .` は永久禁止。** `git add -u`（追跡済みのみ）と `git add workers/*/wrangler.jsonc` のような限定globは可
- **deploy・push・KV書き込み・secret設定は全てTOshiの手**
- **推測で言わない。実物を確認してから発言する**

## デプロイの鉄則（8/9に確立）

**`wrangler deploy` は設定ファイルに書かれていないバインディングを消す。** 必ずこの順で。

1. `npx wrangler deploy --dry-run` でこれから出す内容を見る
2. `npx wrangler deployments status` でバージョンIDを取る
3. `npx wrangler versions view <ID>` で本番の中身を見る
4. **1と3を1行ずつ突き合わせ、一致してから deploy**

**シークレットはこの一覧に出ない。デプロイでも消えない。**（8/9に実測で確認）

**cronは `versions view` では読めない。** ダッシュボードの Settings > Triggers でしか見えない。

---

# 1. 何をやっている会社か

建設・リフォームの見積もりが適正かを、第三者として検証する。売り手からは判定の金を取らない。根拠はDOI登録済みのオープンデータ（JCCDB）と代表の建設実務30年。判定は誰でもSHA-256で再計算でき、Bitcoinにアンカーされている。

**Yakumo** は検証を通った加盟店だけが載る中立モール。紹介料0円・成果報酬なし。
**検証の扉（hs-verify-gate）** はMCPサーバーの適合を測る無料チェッカー。

## 社名の表記（8/9確定）

| 文脈 | 使うもの |
|---|---|
| 日本語・法的表示・schema.org | **The HORIZ音s株式会社**（登記名。法人番号 7021001075279。国税庁の法人番号公表サイトで確認済み） |
| 英文・海外向け | **The HORIZONs Co., Ltd.**（TOshi判断で確定） |

**リポジトリ内は839ファイルが `The HORIZONs株式会社`、17ファイルが `HORIZ音s`。** 17の方が法的表示ページ（tokusho.html / tos.html / index.html）に集中している。**この不一致は未解決。日本語文脈は登記名に揃えるべきだが、839ファイルの一括置換は未実施。**

---

# 2. 売っているもの（8/9に全部値段が付いた）

## 検証済み見積もり（日本の工務店向け）

| | 価格（税込） |
|---|---|
| 初回1件 | 無料 |
| 都度発行 | 1件 ¥3,300 |
| 前払い10件分 | ¥33,000（割引なし） |
| 定額プラン | 月額 ¥21,780（月50件まで） |

**加盟店と非加盟店で価格差なし。月額運用プランに発行件数を含めない。PDFに追加料金なし。施主からの直接依頼は第1弾では受けない。**

決済導線: メール申込 → 銀行振込 / PayPay。**カードは未整備。**
ページ: `/yakumo/kensho-zumi-mitsumori/`

## 監視（海外のMCPサーバー所有者向け）

| | 無料 | 有料 |
|---|---|---|
| **判定** | **同一** | **同一** |
| 公開履歴・掲載 | あり・無料 | あり・無料 |
| 測定頻度 | 週1回 | 毎日 |
| 変化の通知 | なし | webhook |

**$15/月 または $150/年。1契約で3エンドポイントまで。** 日本の消費税は不課税（国外取引）。

**PayPalプラン（稼働中）**
```
monthly  P-9WR34109U23711349NJ36UVQ   $15.00 / MONTH
yearly   P-7115881939415643JNJ36UZQ   $150.00 / YEAR
product  PROD-9SV35182UG370721N
```

ページ: `/verify-directory/monitoring/` と `/verify-directory/monitoring/start/`（購入後）

**有料層への切り替えは今日は手作業。** 購読IDをメールで受けて、`POST /watch` に `x-sweep-token` を付けて `tier: paid` にする。**購入後ページにその旨を正直に書いてある。**

## Yakumo運用プラン（既存）

ライト ¥10,780／スタンダード ¥43,780／プレミアム ¥107,800（月額・税込）。**これは運用費であって成果を売る商品ではない。**

---

# 3. 稼働中のエンドポイント（8/9に独自ドメインへ移行）

```
mcp.horizonshield.dev        hs-mcp              14ツール・paid_by buyer
hearing.horizonshield.dev    hs-hearing          Yakumo共通・paid_by seller・listing_fee true
web.horizonshield.dev        hs-webmcp           paid_by buyer
p001.horizonshield.dev       hs-partner-001-mcp  リフォーム職人株式会社
p002.horizonshield.dev       hs-partner-002-mcp  ミネオトーヨー住器株式会社
gate.horizonshield.dev       hs-verify-gate      検証の扉 v0.2.0
```

**`horizonshield.dev` は Cloudflare Registrar で 2026-08-09 に取得。** 初年度$1、更新$12.20/年、Auto-renew オン、期限 2027-08-09。**切らすと全URLが死ぬ。**

**`workers.dev` も当面は生かしたまま。** 両方が答える。切るなら告知してから。

**独自ドメインを当てていないWorkerが33本ある。** `hs-ledger` `hs-jidec-mcp` `hs-kira-proxy` など。**それらのURLは触っていない。**

**KV**
```
HS_HEARING_KV  cae22b3bf47b46bebdfcdfd6a724f8ab
HS_VERIFY_KV   ab98336fc22d4d0f947501d843d2d228
鍵の置き場     ebeee94b11644031a2deaea32093ac8b
```

**Cloudflare アカウントID**: `c15ff64aba400e541853dec1fbe5e76a`（`oga.surf.project@gmail.com`）

---

# 4. 検証の扉（hs-verify-gate v0.2.0）

## 8/9に足したもの

- **`held` を新設。** 到達できなかった記録は `pending` ではなく `held`。理由つき
- **履歴に `reason` を残す。** 以前は `pass: false` としか残らず、なぜ落ちたか読めなかった
- **`POST /watch`** ... 誰でも自分のエンドポイントを登録できる。`{endpoint, webhook}`。既定は free（週1回・通知なし）
- **`POST /sweep`** ... 掃引の手動実行。`x-sweep-token` が要る。cronを待たずに測れる
- **`GET /sweep/last`** ... 掃引が走ったかどうかが一目で分かる。**「動いているはず」を二度と繰り返さないための計器**
- **通知** ... 条件が反転したら webhook にPOST。どの条件がどっちに変わったかまで入る
- **誤報の抑止** ... 3回連続で到達不能になった時点で、ちょうど1回だけ鳴る。回線の瞬断では鳴らない
- **頻度による課金線** ... free=週1回・paid=毎日。判定は完全に同一

**テスト31件PASS。** 偽のKVと偽のサーバーで、通知・抑止・復旧まで実際に動かして確認済み。

## 絶対に壊してはいけないもの

- **同意ゲート。** 既定でツールを一切呼ばない。`allow_tool_call: true` を明示的に送られたときだけ測る。**自分のサーバーに対してもこれを守っている。だから運営自身も `verified` にならず `pending` のまま**
- **`record_sha256` を変化検知に使わない。** `checked_at` を含むので毎日「変化した」と誤検知する。指紋は `status` と各条件の `pass` だけ（`stateFingerprint`）
- **毎日の掃引に `allow_tool_call` を渡さない。** 毎日走るものが他人のツールを叩いたら監視ではなく攻撃
- **MCPツールの1本目は `get_conditions`（引数なし・読み取り専用）。** 意図的な順序
- **`MAX_PER_SWEEP = 15`。** 上限で落ちた分は `skipped` に理由つきで必ず記録する。黙って切ると「全部測った」と読める。**有料プラン確定済みなので、必要なら3,000本規模まで上げられる**

## 未了

**扉は自分自身に到達できない。** `gate.horizonshield.dev` を自分で fetch すると `http 522`。Workerは自分のホスト名を引けない。**カスタムドメインでも解けない。** 監視リストには入れたまま、理由つきで `held` を記録している。**落ちる方のエントリを黙って外さない、という方針。**

---

# 5. Workerの設定（8/9に15本を本番と一致させた）

**7/23に一括生成された15本の `wrangler.jsonc` にプレースホルダーが残っていた。** deployすれば本番の環境変数が置換用文字列で潰れ、cronが消える状態だった。**8/9に全部、本番の実測値に書き戻した。**

**判明したこと**

- **`compatibility_flags: ["nodejs_compat"]` がプレースホルダーで全部に入っていた。** 本番で実際に付いているのは `hs-kira-line` の1本だけ。**14本に付けてデプロイしていたらランタイムの挙動が変わっていた**
- **「varsのプレースホルダー」の正体はほとんどSecretだった。** `LINE_USER_ID` `ALERT_FEED_URLS` `APIFY_ACTOR_ID` `ALERT_LINE_TO`。vars に書けばSecretの隣に平文が生える
- **`hs-kira-line` はすでに事故済みだった**（次章）

**cron一覧（実測）**
```
hs-genka-ingest    0 3 1 * *        毎月1日 03:00
hs-internal-mcp    */30 * * * *     30分ごと
hs-price-sync      0 13 10 * *      毎月10日 13:00
hs-kira-line       0 * * * *        毎時0分
hs-hearing         17 21 * * *
hs-verify-gate     0 18 * * *       UTC = JST 03:00
hs-monitor         */30 * * * *
hs-pdf-gen         */30 * * * *
hs-outreach        0 0-8 * * 1-5
hs-souba-pipeline  0 21 * * * / 0 9 * * *
```

**★ 3本が `scheduled()` を持つのに cron が1本も無い。** `horizon-shield-kira` `hs-blog-post` `hs-followup`。**一度も走っていない。足すか要らないかはTOshiの判断待ち。**

---

# 6. hs-kira-line（8/9に復旧）

**本番の `BANK_INFO` と `LINE_USER_ID` に、置換用の日本語文字列がそのまま入っていた。** 過去にその状態で deploy されている。

- **`LINE_USER_ID`** ... 代表への push 通知の宛先。コード内8箇所。**一通も届いていなかった**
- **`BANK_INFO`** ... 顧客に案内する振込先。**顧客に置換用文字列が案内されていた可能性がある**

**直した内容**

- `BANK_INFO` を実値に（`静岡銀行 平塚支店 普通 0479810 The HORIZ音s株式会社`）
- `LINE_USER_ID` を vars から外し、**Secretに寄せた**（他5本のWorkerと同じ扱い）
- cron `0 * * * *` を設定ファイルに書き戻し
- **`POST /diagnosis` で実際にLINEが届くことを確認済み**（認証なし・副作用なし・代表宛に1通飛ぶだけ。テストに使える）

**同時に直ったもの**: `hs-kira-line` の2箇所が同一アカウントの `workers.dev` を叩いていて、実は届いていなかった。今は `mcp.horizonshield.dev/api/search` と `hearing.horizonshield.dev/kira-bridge`。

**同じ罠が `hs-hearing` にもあった。** `STATS_SINK` が `hs-webmcp.oga-surf-project.workers.dev/beacon` を叩いていた。今は `web.horizonshield.dev/beacon`。**加盟店に見せるAI面の露出実数が、今日から本当に記録され始める。**

---

# 7. 掲載先とレジストリ

## 公式MCPレジストリ（8/9に全部更新）

```
io.github.ogasurfproject-jpg/horizon-shield          1.0.4  mcp.horizonshield.dev
io.github.ogasurfproject-jpg/horizon-shield-webmcp   1.0.2  web.horizonshield.dev/mcp
io.github.ogasurfproject-jpg/hs-hearing              2.2.0  hearing.horizonshield.dev/mcp
io.github.ogasurfproject-jpg/hs-verify-gate          0.2.0  gate.horizonshield.dev/mcp
io.github.ogasurfproject-jpg/hs-audit-app            0.1.0  hs-audit-app...workers.dev  ★
```

**公開手順**
```
mcp-publisher login github
cd <server.jsonのあるディレクトリ> && mcp-publisher publish
```

**罠が2つ**
- **`description` は100文字まで。** 超えると 422 で弾かれる。8/9に2回踏んだ
- **同じバージョンは再公開できない。** 必ず version を上げる
- **トークンは期限切れする。** 401 が出たら `mcp-publisher login github` から

## 他の掲載先

| 掲載先 | 経路 | 8/9の状態 |
|---|---|---|
| Anthropic MCP Directory | `directory@mail.anthropic.com`（チケット #117309234） | **審査中。URL変更を8/9に通知済み** |
| Glama | **`frank@glama.ai` に手で再インデックス依頼。自動同期ではない** | 8/9に依頼済み。**反映を確認すること** |
| mcpservers.org | `contact@mcpservers.org` | 8/9に更新依頼済み |
| Smithery | **経路不明。メール履歴なし** | 掲載ページ `smithery.ai/server/oga-surf-project/horizon-shield` |

---

# 8. 次にやること（優先順）

## A. パスワード【最優先・未着手】

8/8にTOshiがMacのログインパスワードをチャット欄に貼った。**漏洩したものとして扱う。** macOSのログインパスワードと、同じ文字列を使い回している全サービスを変更する。**8/8に1回、8/9に11回聞いて、いずれも回答が無い。**

**変更は必ずシステム設定から。** ターミナルの `passwd` を使うとキーチェーンとずれる。

**変える順**: iCloud → GitHub → Cloudflare → PayPal → 銀行。**GitHub・Cloudflare・PayPalの2要素認証も確認する。**

## B. 決済の実測【未着手】

**ボタンは動くが、まだ誰も払っていない。** 日本のPayPalアカウントでドルを受け取れるか、手数料がいくら引かれるかは未検証。**フェデリコに$15を1回払ってもらえば全部分かる。** ディレクトリを送る用事もある。

## C. `hs-audit-app` の身元【未解決】

**Cloudflareに実在し（id `4a075820e88b41e88fd21fe43e026423`）、エンドポイントも応答し、公式レジストリで公開されているのに、ソースが `workers/` に無い。** 直すことも作り直すこともできない。**TOshiに在処を聞く。**

## D. 外部の1件目

**登録はまだ全部自社。** 誘い文句は8/9に変わった。「掲載しませんか」ではなく**「あなたのサーバーを毎日測って、条件が落ちたら知らせます。無料です」**。断る自由を先に渡す形の英文を用意済み（本文は下記）。

**候補（公式レジストリから実測）**
```
https://mcp.goji.agency/mcp
https://mcp.lona.agency/mcp
https://mcp.aarna.ai/mcp
https://api.adside.ai/mcp
https://mcp.adweave.ai/meta-ads-mcp
```

**最初は3件だけ。** Glamaの実測が表示54,911・クリック24・Tool Calls 0。数を撃っても当たらないことは分かっている。

## E. 残りの技術的判断

- **3本のcron**（`horizon-shield-kira` `hs-blog-post` `hs-followup`）を足すか要らないか
- **LINEのユーザーIDが公開リポジトリに平文で2箇所**（`workers/hs-pdf-gen/src/worker.js` と `workers/hs-pdf-gen-v12.js`）。Secretに寄せて消すべき
- **特商法の記載**に「検証済み見積もり定額プラン: PayPal定期課金」とあるが、そのPayPalプランはまだ作っていない
- **839ファイルの社名表記**を日本語文脈だけ登記名に揃えるか

## F. 月曜の巡回結果

**8/8に投入した227ページと検証ディレクトリの答え合わせ。** CITED 0/15 が動くか。手動なら `~/Desktop/horizon-shield/run_visibility_weekly.sh`。**番人からは `~/Desktop/` が見えない。TOshiに実行してもらい出力を貼ってもらう。**

---

# 9. 実測データ（8/8時点。8/9は測っていない）

**LLM可視率 CITED 0/15。** 7/7から不変。8/8投入の227ページはこの測定時点で未読。

**初の被引用**: Perplexityが1件。「建設・リフォームの見積もりを第三者にチェックしてもらえるサービスは？」で shield トップを引いた。**言い換えでは消える。定着していない。**

**引用されない構造的理由**（実測で特定）
1. 勝っているのは「1問1ページ」の解説記事
2. 唯一引かれたのはトップページ。LLMは「サービス」としか認識していない
3. ChatGPTとClaudeは多くの問いでweb検索を発動しない
4. **勝っている側の相場表には出典がない。条件別に数字を並べているだけで勝っている → LLMは主張ではなく数字を引用する**

**MCP開示率サーベイ**: 自社6/6・100%（8/8夜）。**朝の初回は 0.0% だった。**

**Glama**: 表示54,911・クリック24（CTR 0.0%）・プロフィール閲覧412・**Tool Calls 0**。

---

# 10. 保留・未送信

- **LinkedIn**: 論文7814の投稿はまだ（文面4種と画像は完成済み）
- **Smithery サポート**: Arcade.dev買収に伴うメトリクス断絶の確認。返事待ち・経路不明
- **Apify**: 月間上限到達済み
- **堤さん（加盟No.001）**: 11回質問して回答ゼロ、17日沈黙。**連絡先はこのファイルに無い。TOshiに聞く**
- **フェデリコ**: composed-attestation の working group。ディレクトリを送れる状態。**連絡手段はこのファイルに無い**
- **セキュリティ**: 8章A参照。**未完了**

---

# 11. 残しておくべき原則

**扉は自分にも同じ基準を当てる。** 自分の物差しで自分を測ったら0.0%で落ちた。**8/9時点でも運営自身は `verified` ではなく `pending`。同意なしにツールを呼ばない規則を、自分にも適用しているから。** 運営だけ特別扱いすれば緑にできるが、それをやった瞬間に物差しが物差しでなくなる。

**厳しいのは形式についてだけ。中身は意図的に裁かない。** 「誰が金を払っているか宣言せよ」とは要求するが、その内容の良し悪しは判定しない。**裁くのは読み手であって、扉ではない。**

**事実を先に変え、それから宣言を変える。** 8/9はこれを実際にやった。カスタムドメインで届くようになった（事実）→ ディレクトリの「同一アカウントなので届かない」を訂正した（宣言）。**8/8の記録は消さず、「8/8まではこうだった、8/9にこう変わった」と並べた。**

**都合の悪い事実を書いても通る設計にしておく。** `hs-hearing` は `listing_fee: true` と正直に書いた上で適合している。

**揃えたくなっても揃えない。** 監査は買い手の金、モールは売り手の金。資金源が違うなら違うまま書く。

**判定を売らない。売るのは頻度と通知だけ。** 監視商品はこの線の上に立っている。**判定に値段が付いた瞬間、全部崩れる。**

**誤報を1回でも出した監視に、二度目の金は払われない。** 3回連続の到達不能まで鳴らさないのは、この一行のため。

**金で買えないものを軸にする。** DOI登録済みオープンデータ・査読前プレプリント・ORCID著者性・30年の現場・暗号的再計算可能性。**そして8/9に1つ増えた。公開履歴の日数。** 今日が1日目。

**人間にしか読めない開示は、開示ではない。**

**「実装した」は「動いている」ではない。** 8/9に半日を使って学んだ。走ったことを自分で証明する計器を、必ず一緒に作る。

以上。
