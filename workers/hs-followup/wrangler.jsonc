{
  // 2026-08-09 ダッシュボード実測: scheduled() ハンドラはあるが cron が1本も無い。
  // つまり定期実行は一度も起きていない。動かしたいなら cron を足すこと。
  // 現状に合わせて triggers は書かない（書くと今無いものを作ることになる）。
  // live照合済み。Secret も vars も無し。KV は FOLLOWUP のみ。
  "name": "hs-followup",
  "main": "src/worker.js",
  "preview_urls": false,
  "observability": { "enabled": true, "logs": { "enabled": true } },
  "compatibility_date": "2026-05-12",
  "kv_namespaces": [
    { "binding": "FOLLOWUP", "id": "bb1da83e30914ea998eaeadbe389c79c" }
  ]
}
