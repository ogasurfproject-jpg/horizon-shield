{
  // 2026-08-09 ダッシュボード実測: scheduled() ハンドラはあるが cron が1本も無い。
  // つまり定期実行は一度も起きていない。動かしたいなら cron を足すこと。
  // 現状に合わせて triggers は書かない（書くと今無いものを作ることになる）。
  // live照合済み。バインディング無し。ALERT_FEED_URLS / ANTHROPIC_API_KEY /
  // LINE_CHANNEL_TOKEN / LINE_USER_ID は全部 Secret なので vars には置かない。
  "name": "horizon-shield-kira",
  "main": "src/worker.js",
  "compatibility_date": "2026-03-26"
}
