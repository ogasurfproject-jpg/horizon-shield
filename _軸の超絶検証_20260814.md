# 4軸の超絶検証 — 2026-08-14

**独立した2人の敵対的レビュアーに、別々の角度から攻撃させた。** 片方は「どう偽装するか」、片方は「正直な運営者をどう不当に罰するか」。**互いの結論を見せていない。**

**それでも、2人とも同じ結論に着いた。**

---

# 0. 結論を先に

**俺が出した4軸のうち、2本は捨てる。1本は作り直す。1本は改名して大量の機械を足す。そして5本目が要る。**

| 軸 | 判定 |
|---|---|
| A 可用性 | **改名して存続。** ただし条件付き。やらないなら捨てる |
| B 修復速度 | **★削除。2人とも独立に「これが最悪」と言った** |
| C 表面の安定性 | **指標としては削除。日付付きの差分だけ残す** |
| D エラーの形 | **一律の列からは削除。同意した相手にだけ、全文つきで** |
| **E 提示の一貫性** | **★新規。唯一「ズルの防ぎ方が、ズルをやめること」になる軸** |

---

# 1. 一番でかい発見 — 「順位を出さない」は、もう破れている

俺は「順位を作らなければ買収されない」と書いた。**2人とも、独立に、そこを最初に殴ってきた。**

> "You do not create a ranking by sorting; you create one by publishing comparable scalars next to each other. `availability 86%`, `median repair 3 days` are comparable scalars. Declining to render a leaderboard is a cosmetic disclaimer, not a design property."

**並べた時点で順位である。** 一覧を並べ替えなくても、**最初のスクレイパーが並べ替えて、こちらのデータを出典に leaderboard を公開する。**

**したがって規則はこうなる:**

> **順序を持つ出力を publish しない。** 割合も、中央値も、連続日数も、回数も。
> **許されるのは、順序を持たないものだけ** — boolean、日付付きの事象の列、ハッシュ、そして分母つきの実数（「52回中47回」であって「90.4%」ではない）。

**A と B は、免責文を着た順位そのものだった。**

---

# 2. もう1つ、致命的なやつ

> "The endpoint that scores best on all four is: unauthenticated, no WAF, no rate limit, always-warm, single-region, single-version, never changed. **That is a demo.** The endpoint that scores worst is: OAuth-protected, WAF'd, rate-limited, scale-to-zero, multi-region, canary-deployed, actively maintained. **That is a real service.**"

**4軸とも、本番の成熟度を欠陥として読む。**

- WAF / bot 対策が入っている → 403 → 「到達不能」
- レート制限が入っている → 429 → 分類不能
- scale-to-zero（小さい事業者の正しい選択） → **毎回コールドスタートしか観測できない**
- カナリアリリース中 → tools/list が日替わり → 「表面が不安定」
- 認証必須（MCPの認証モデルどおり） → 401 → 「エラーの形が不正」

**そして最悪なのはこれだ。** 可用性の軸は、**知らないスキャナを allowlist した運営者に高い点を出す。** つまり**「見栄えを良くするためにセキュリティを緩めろ」という公開圧力**をこちらがかけることになる。

---

# 3. 軸ごとの判定

## B 修復速度 — ★削除。議論の余地なし

**2人とも独立に「これを最初に消せ」と言った。** 理由が3つあり、どれも単独で致命的。

**(1) 分解能の床が、そのままサンプリング間隔になる**

20分で直した修理が「1日」と公開される。**週1回の無料層なら「7日」** — 500倍の誇張を、事実として、永久に。**まともな運営者の実力差は全部この床の下にあるので、公開される差は100%こちらの cron が撃った時刻の artifact になる。**

**(2) 数字を養殖できる**

修復速度には「壊れた記録」が入力として要る。**だから見栄えを良くしたい運営者は、壊れた記録を自分で作る。**

```
条件を1個、掃引の前に落として、後で戻す。crontab 2行。
四半期に10回やれば、永久の公開記録に
「median repair latency: 1 measurement, n=10 — 非常に迅速な運営者」と載る。
```

**一度も壊していない運営者は n=0 で、記録が空になる。** 自作自演した側の記録のほうが立派に見える。**しかもその記録は、この事業の原則により、消せない。**

**(3) この事業の正直さを、この軸が直接罰する**

> **扉自身の `pending` は、直らない。** 自分のホスト名に自分から届かないから測れず、測っていないものを合格にしないと決めたからだ。
> **修復速度の軸では、それは「無限に伸び続ける未修復」として、最悪の記録になる。**

自分だけ例外にすれば self-dealing。例外にしなければ、**この事業でいちばん正直な振る舞いが、この事業でいちばん大きい欠陥として公開される。**

そして「こちらの検査が間違っているので直さない」と判断した運営者にも、日々増える数字が付く。**「あなたの検査に異議がある」を「あの会社は怠慢だ」に変換する装置**になる。**議論する気のある相手に、いちばん強く効く。** 選別したい性質の、正反対だ。

**→ 削除。日付付きの flip 事象は既に history にあるので、それをそのまま見せる。集計値は一切作らない。**

## C 表面の安定性 — 指標としては削除。差分だけ残す

**俺の案は、いちばん壊れやすい変更を構造的に見落とす。**

> **ツール名だけをハッシュしている。** `get_price_range` という名前を永久に置いたまま、inputSchema を書き換え、必須パラメータを足し、出力の形を変えられる。**ハッシュは1ビットも動かない。**
> **既存ツールに必須パラメータを足すのは、現実の統合が壊れる原因の第1位である。そこがこの指標の構造的な盲点だ。**

**偽装コストはゼロ。何もしなくていい。**

そして「安定性」という名前が価値判断を含んでいる:

> **この軸で満点を取るのは、放置されたサーバーである。**

400日ハッシュが動かない = 模範的。肥大したツールを2本に割り、非推奨を正しく畳んだ運営者 = 「churn」。**MCP 仕様自体が `notifications/tools/list_changed` を一級市民として持っている。仕様の作者はツール一覧の変化を正常な運用と見なしている。**

さらに、**この指標は良いリファクタリングを抑止する。** 記録を気にする運営者ほど、死んだツール名を永久に残すようになる。

**→ 「安定性」という言葉、変化回数、無変化日数、割合、全部捨てる。残すのは日付付きの中立な差分1行だけ。**

```
2026-03-04: tools/list に X を観測。Y は観測されなくなった。
```

**そして測る対象を、名前からマニフェスト全体に変える。** name + description + inputSchema を正規化（JCS）してハッシュし、変化の種別だけ出す（追加／削除／inputSchema変更／必須パラメータ変更）。**ツールは1本も呼ばない。内容の良し悪しも判定しない。順序も付かない。**

**★必須の防御: `tools/list` はカーソルでページングされる。** ページ境界をまたいだだけで **「14本のツールが削除された」** と公開してしまう。**カーソルを最後まで辿り、辿り切れなかった掃引は UNKNOWN にする。部分的な読み取りから削除を出してはいけない。**

## D エラーの形 — 一律の列からは削除

**(1) 公開ソースから1行コピーすれば、永久に突破される**

```js
if (req.method === "hs_probe_nonexistent_method")
  return { jsonrpc:"2.0", id:req.id, error:{ code:-32601, message:"Method not found" } };
```

**プローブ名がリポジトリの定数だ。** これを書けば、他のあらゆる未知メソッドに 200 を返し続けても永久に合格する。

**(2) この事業の同意の原則に反している**

> "It is deliberately invalid input sent to a stranger's production system, unauthenticated, daily, from a Cloudflare IP. It fills their error logs, may burn error-budget SLOs, may page someone at 3am, and is indistinguishable from reconnaissance."

**「ツールを呼んでいないから同意は要らない」は、文言は満たすが精神に反する。**

**(3) 正しい実装ほど落ちる**

- **認証を先に見るサーバー**（MCP の認証モデルどおり）は 401 を返す。**それが正解なのに「-32601 を返さなかった」と記録される**
- セッション必須のサーバーは 400 を返す。正解
- **`id` の無い JSON-RPC は notification で、仕様上サーバーは応答してはいけない。** プローブが `id` を落とせば、正しいサーバーが全部落ちる
- **HTTP 200 に正しい JSON-RPC エラー本文を載せるのは正解である**（JSON-RPC はエラーを本文で運ぶ）
- プロトコル改定（2024-11-05 / 2025-03-26 / 2025-06-18 / 2025-11-25）で期待が変わる。**同じ boolean が、日付によって別の意味になる。時系列記録の前提が崩れる**

**(4) 予算に入らない**

`wrangler.jsonc` に「1本につき外部アクセス3回。Free の 50/request に収まる上限」と書いてあり、`MAX_PER_SWEEP = 15`。**15 × 3 = 45。4本目を足すと 15 × 4 = 60 で、上限 50 を超える。** 掃引が壊れる。

**→ 一律の列から外す。同意した相手にだけ、送った要求と返ってきた応答を全文添えて、判定は「明白なときだけ」。曖昧なら FAIL ではなく NOT MEASURED。**

**ただし、救う道が1つある。** プローブ名を毎回ランダムにすれば:

> **残る唯一の防御が `if (!knownMethods.includes(m)) return -32601` になる。それは正しい実装そのものである。ズルの方法が、ズルをやめることに潰れる。**

**これが、良い軸の見分け方だ。**

## A 可用性 — 改名して存続。ただし条件付き

**単発の失敗を記録してはいけない。** 1回の TCP reset、1回の DNS SERVFAIL、Cloudflare の PoP 障害。**1リクエストあたり 0.1〜1% は普通に落ちる。それは可用性100%のサーバーに、年1〜4回の捏造された「障害日」を作る。週1回の層なら、1回のまばたきが「86%」になる。**

**存続の条件（全部やる。1つでも欠けたら軸ごと捨てる）:**

| # | やること | 理由 |
|---|---|---|
| 1 | **失敗したら10分以上あけて2〜3回再確認してから記録** | 1%の瞬断が 1e-6 になる。これをやらずに「可用性」を名指しの他社について公開するのは、いちばん名誉毀損に近い |
| 2 | **掃引時刻をジッタさせる**（`HMAC(secret, endpoint‖date)` で分を導出し、実行後に実時刻を公開） | cron `0 18 * * *` は運営者に読まれている。**しかも 03:00 UTC に定時メンテを持つ優良運営者と衝突すれば、永久に 0% になる** |
| 3 | **既知正常のカナリアを毎回測る。カナリアが落ちたら掃引全体を VOID** | こちらの障害を相手の記録にしない。Cloudflare の cron は best-effort で、飛ぶこともある |
| 4 | **失敗を分類する**（DNS / TLS / connect timeout / 403-WAF / 429 / 5xx / GATE_ERROR） | **「落ちている」と「こちらを弾いた」は別の事実。** いま混ざっている |
| 5 | **割合を出さない。「52回中47回」と回数で出す。連続失敗はサンプル数でのみ表現し、日数に換算しない** | 1日1回の観測で `streak: 2` は「24時間強〜72時間弱」。**週1なら「8日〜21日」。** 整数は無い精度を主張する |
| 6 | **名前を変える。`availability` ではなく `probe reachability from a single vantage`** | 1日1回の突きから出した「99.7% uptime」は、各サンプルが正直でも合成の嘘である |
| 7 | **`cf.colo` を記録して公開** | cron の Worker は毎回同じ colo から出ない。**地域限定サービスは、こちらのスケジューラの都合で可用性が乱数になる** |

**1〜3をやらないなら、この軸は出さないほうがいい。**

---

# 4. ★E 提示の一貫性 — 新規。これが本命

敵対レビュー側から出てきた、**4軸のどれよりも偽装が難しい軸。**

**やること**: 毎回、同じ read-only の対象（`initialize` / `tools/list` / agent card）を**3つの視点**から取り、バイト単位で比較する。

- (a) 名乗った扉の UA、Cloudflare Worker から
- (b) 名乗らない一般的なクライアント、同じ場所から
- (c) **名乗らないクライアント、Cloudflare の外から**（VPS / GitHub Actions）

**出力は boolean 1つと、差異の種別だけ。** 「このサーバーは、3つの見え方に同じ表面を提示したか」

**なぜこれが強いか:**

> **正直な運営者は、何もしなくても通る。**
> **細工を1つでも入れている運営者は、自動的に落ちる。**
> **通すための唯一の方法が、細工をやめることである。**

**A〜D は全部この試験に落ちる。** そして上で挙げた偽装 — UA で tools/list を出し分ける、`hs_probe_*` を特別扱いする、稼働時間を allowlist で作る — **全部がこの軸で自己申告になる。**

**★(c) が必須である理由（実測で確認すべき）**

> Cloudflare は Worker の subrequest に **`CF-Worker` ヘッダ（発信元ゾーン名）** を付ける。**UA を消しても、相手が Cloudflare 配下なら扉のゾーンが割れる。**

**確認方法**: 自分で持っているログ用エンドポイントに掃引を1回撃って、届いたヘッダを読む。**`CF-Worker` があれば、2つ目の視点は Cloudflare の外に置かないと意味がない。** GitHub Actions の cron からで足りる。

**透明性との整合**: 名乗らないクライアントを使うのは欺瞞ではない。**「扉は名乗る視点と名乗らない視点の両方から取り、突き合わせる」と方法論に書いてあれば、方法は透明で、個々の実行だけが予測不能という形になる。** ジッタと同じ理屈だ。**特定の第三者になりすますのは別問題なので、それはやらない。**

---

# 5. fajar への返信を差し替える

**★まだ送っていないなら、前に渡した文は送るな。** あれは B と D を売り物にしていた。**両方とも、いま検証で落ちた。**

```
Yes — but not by ranking, and I had to throw out half of my own answer to
say why.

I sat down to design "verified outcome" axes and two of the four died on
inspection. Repair latency is farmable: you manufacture failures, then fix
them fast, and the register hands you a permanent record of how responsive
you are. And any scalar we publish — a percentage, a median — is a rank
whether or not we sort the page. Someone else sorts it and cites us.

What survived is the shape where the only way to fake a pass is to stop
faking: fetch the same read-only surface as an identified client and as an
anonymous one, from two different networks, and compare the bytes. An
honest server passes for free. Anything that special-cases a scanner fails
itself.

Does your side of this look the same, or am I missing a class of outcome
that can't be gamed?
```

**498字。Threads の上限内。**

**自分の案が半分死んだと先に言っている。** それがこの事業でいちばん強い形だからだ。

---

# 6. 次の一手

**今日はコードを1行も触らない。** 4軸のうち2本を捨てる判断が出たばかりで、残る2本にも大量の前提条件が付いた。**この状態で書き始めるのが、いちばんまずい。**

**やるなら、この順:**

| # | 手 | 中身 |
|---|---|---|
| 1 | **`CF-Worker` を実測** | ログ用エンドポイントに掃引を1回。**ヘッダが漏れているかどうかで、E の設計が根本から変わる** |
| 2 | **C の差分だけ実装** | `summarise()` にマニフェストのハッシュを1つ足す。**カーソルを辿り切る。辿れなければ UNKNOWN。** 指標は作らない |
| 3 | **A の 1〜3**（再確認・ジッタ・カナリアVOID） | これが無いと A は出せない |
| 4 | **E を GitHub Actions で** | Cloudflare の外の視点。無料 |
| 5 | D | 同意した相手にだけ。プローブ名はランダム |

**B は実装しない。**

---

# 7. 今日いちばん残すべき一文

**俺は4本出して、2本が死んだ。**

そして死んだ理由が、両方とも同じだった。**測ったものより大きいことを言う形をしていた。**

- 修復速度は、cron が撃った時刻を「運営者の実力」として公開する形
- 割合と中央値は、「順位は出しません」と書きながら順位を配る形

**扉の `pass: true` と、`/sse` の 200 と、まったく同じだ。**

**今日5回目である。**
